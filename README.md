# Landing — Taller de [NOMBRE DE LA PSICÓLOGA]

React + Vite + TypeScript + Tailwind CSS. Sin librerías externas.
Todo el contenido visible vive en `src/App.tsx`.

## Correr

```bash
npm install
npm run dev
```

## Placeholders

Todo lo que está [ENTRE CORCHETES] se reemplaza con las respuestas del cuestionario:

- Beneficios (`featureBars`): pregunta 4
- Datos duros (`datosTaller`): preguntas 5–13
- Fotos (`HERO_IMAGE`, `SECTION2_IMAGE`, `SECTION3_IMG1/2`, `SECTION3_BG`): preguntas 19–21
- `WHATSAPP_URL`: número con código de país (ej. `https://wa.me/5491122334455`)
